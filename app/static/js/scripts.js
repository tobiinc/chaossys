'use strict';function algorithm() {    var contentClean = $('#content').val().replace(/[^a-z_\s]/gi, '').replace(/[_\s]/g, ''),        contentArray = [],        count = $('#value').val(),        resultCount = [];    for (var i = -1; i < contentClean.length; i++) {        contentArray.push(contentClean.charAt(i));    }    if (contentClean.length == 0) {        alert("Content is required");        return;    }    if (count.length == 0) {        alert("Value is required");        return;    }    if (count >= contentArray.length) {        alert("value is more than content!");        return;    }    while (resultCount.length < count) {        var randomnumber = Math.ceil(Math.random() * contentClean.length);        var found = false;        for (var i = 0; i < contentClean.length; i++) {            if (resultCount[i] == randomnumber) {                found = true;                break;            }        }        if (!found)resultCount[resultCount.length] = randomnumber;    }    var result = contentArray.filter(function (letter, count) {        return resultCount.indexOf(count) !== -1;    });    for (var i = 0; i < result.length; i++) {        $('#result').html(result);    }}function layoutClicker() {    $("#byInput").click(function() {        $('.tabs li').removeClass('active');        $(this).addClass('active');        $('.inputWrapper').addClass('hidden');        $('.byInput').removeClass('hidden');    })    $("#byWeb").click(function() {        $('.tabs li').removeClass('active');        $(this).addClass('active');        $('.inputWrapper').addClass('hidden');        $('.byWeb').removeClass('hidden');    })    $("#byGenerated").click(function() {        $('.tabs li').removeClass('active');        $(this).addClass('active');        $('.inputWrapper').addClass('hidden');        $('.byGenerated').removeClass('hidden');    })}function random() {    var text,        possible = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz';    for(var i=0; i < 10; i++ ){        text += possible.charAt(Math.floor(Math.random() * possible.length))+ ',';    }    console.log('te '+text);/*    http://stackoverflow.com/questions/1349404/generate-a-string-of-5-random-characters-in-javascript */}function randomString() {    var chars = "ABCDEFGHIJKLMNOPQRSTUVWXTZabcdefghiklmnopqrstuvwxyz";    var string_length = 800;    var randomstring = '';    for (var i=0; i<string_length; i++) {        var rnum = Math.floor(Math.random() * chars.length);        randomstring += chars.substring(rnum,rnum+1);    }    console.log(randomstring);/*    http://www.mediacollege.com/internet/javascript/number/random.html */}function click() {    var numCount = 100,        lenCount = 10;    $.get('https://www.random.org/strings/?num='+ numCount + '&len=' + lenCount + '&upperalpha=on&loweralpha=on&unique=on&format=plain&rnd=new', function(data){        console.log(data);    });}$("#btn").click(function() {/*        click(); */        algorithm();});        layoutClicker();        randomString();