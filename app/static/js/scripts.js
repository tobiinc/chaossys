'use strict';function algorithm() {    var contentClean = document.getElementById("content").value.replace(/[^a-z0-9_\s]/gi, '').replace(/[_\s]/g, '');    var contentArray = [];    var count = document.getElementById("value").value;    var resultCount = [];    for (var i = -1; i < contentClean.length; i++) {        contentArray.push(contentClean.charAt(i));    }    if (count >= contentArray.length) {        alert("value is more than content!");        return;    }    while (resultCount.length < count) {        var randomnumber = Math.ceil(Math.random() * contentClean.length);        var found = false;        for (var i = 0; i < contentClean.length; i++) {            if (resultCount[i] == randomnumber) {                found = true;                break;            }        }        if (!found)resultCount[resultCount.length] = randomnumber;    }    var result = contentArray.filter(function (letter, count) {        return resultCount.indexOf(count) !== -1;    });    document.getElementById("wrapper").innerHTML = result.toString();    console.log('contentArray: ' + contentArray.length);    console.log('contentClean: ' + contentClean);    console.log('contentCleanLength: ' + contentClean.length);    console.log('count: ' + count);    console.log('resultCount: ' + resultCount);    console.log('result: ' + result.toString());    console.log('value: ' + document.getElementById("value").value);}(function () {    document.getElementById("btn").onclick = function() {algorithm()};})();